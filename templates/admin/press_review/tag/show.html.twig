{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_press_review_tag_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_press_review_tag_edit', {'id': tag.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/press_review/tag/_delete_form.html.twig') }}
{% endblock %}

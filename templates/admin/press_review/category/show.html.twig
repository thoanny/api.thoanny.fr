{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_press_review_category_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_press_review_category_edit', {'id': category.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/press_review/category/_delete_form.html.twig') }}
{% endblock %}

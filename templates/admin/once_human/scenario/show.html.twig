{% extends 'base.html.twig' %}

{% block title %}Scenario{% endblock %}

{% block body %}
    <h1>Scenario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scenario.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scenario.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ scenario.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_once_human_scenario_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_once_human_scenario_edit', {'id': scenario.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/once_human/scenario/_delete_form.html.twig') }}
{% endblock %}

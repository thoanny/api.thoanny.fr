<form method="post" action="{{ path('app_admin_once_human_scenario_delete', {'id': scenario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scenario.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

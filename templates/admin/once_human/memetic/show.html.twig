{% extends 'base.html.twig' %}

{% block title %}Memetic{% endblock %}

{% block body %}
    <h1>Memetic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ memetic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ memetic.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ memetic.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_once_human_memetic_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_once_human_memetic_edit', {'id': memetic.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/once_human/memetic/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}ItemCategory{% endblock %}

{% block body %}
    <h1>ItemCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_once_human_item_category_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_once_human_item_category_edit', {'id': item_category.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/once_human/item_category/_delete_form.html.twig') }}
{% endblock %}

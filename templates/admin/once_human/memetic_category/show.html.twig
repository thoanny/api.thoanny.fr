{% extends 'base.html.twig' %}

{% block title %}MemeticCategory{% endblock %}

{% block body %}
    <h1>MemeticCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ memetic_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ memetic_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_once_human_memetic_category_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_once_human_memetic_category_edit', {'id': memetic_category.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/once_human/memetic_category/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}SpecializationGroup{% endblock %}

{% block body %}
    <h1>SpecializationGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialization_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialization_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_once_human_specialization_group_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_once_human_specialization_group_edit', {'id': specialization_group.id}) }}" class="btn btn-primary">edit</a>

    {{ include('admin/once_human/specialization_group/_delete_form.html.twig') }}
{% endblock %}

{% extends '../base.html.twig' %}

{% block title %}Hello World!{% endblock %}

{% block body %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div>
            Hello World!
        </div>
    {% else %}
        <div class="d-inline-flex gap-2">
            <a href="https://enshrouded.vercel.app" target="_blank" class="btn btn-primary">Enshrouded <i class="bi bi-box-arrow-up-right"></i></a>
            <a href="https://once-human.thoanny.fr" target="_blank" class="btn btn-primary">Once Human <i class="bi bi-box-arrow-up-right"></i></a>
            <a href="https://palia.thoanny.fr" target="_blank" class="btn btn-primary">Palia <i class="bi bi-box-arrow-up-right"></i></a>
        </div>
    {% endif %}
{% endblock %}
